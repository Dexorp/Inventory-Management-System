
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ims-api.fullname" . }}
  labels:
    {{- include "ims-api.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ims-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP