apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ims-api.fullname" . }}-secret
  labels:
    {{- include "ims-api.labels" . | nindent 4 }}
type: Opaque
stringData:
{{- range $k, $v := .Values.secretEnv }}
  {{ $k }}: {{ $v | quote }}
{{- end }}