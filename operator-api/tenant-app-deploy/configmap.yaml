apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ims.configMapName" . }}
  labels:
    {{- include "ims.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}